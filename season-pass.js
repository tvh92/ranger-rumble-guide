const $=selector=>document.querySelector(selector),version=document.querySelector('meta[name="app-version"]')?.content||'dev',assetUrl=path=>`images/${path}?v=${encodeURIComponent(version)}`,seasonAssetUrl=path=>assetUrl(`season_pass/${path}`),safe=value=>{const element=document.createElement('span');element.textContent=value;return element.innerHTML;};
function seasonRewardIcon(reward){if(/Reebo \(Epic\)/i.test(reward))return'gadgetron_body_epic.png';if(/Reebo \(Rare\)/i.test(reward))return'gadgetron_body_rare.png';if(/Reebo \(Common\)/i.test(reward))return'gadgetron_body.png';if(/Hero Lorbs \(Common\)/i.test(reward))return'ico_Lorb_common.png';if(/Hero Lorbs \(Rare\)/i.test(reward))return'ico_Lorb_rare.png';if(/Hero Lorbs \(Epic\)/i.test(reward))return'ico_Lorb_epic.png';if(/Qredits/i.test(reward))return'ico_credit02.png';if(/Cores/i.test(reward))return'ico_core.png';if(/Skin: Raritanium Chip/i.test(reward))return'../skins/Chip/Raritanium%20Chip.png';if(/Raritanium/i.test(reward))return'ico_Raritanium.png';if(/Fashionium/i.test(reward))return'ico_Fashionium.png';return null;}
function seasonReward(reward){const iconFile=seasonRewardIcon(reward),iconMarkup=iconFile?`<img src="${iconFile.startsWith('../')?assetUrl(iconFile.slice(3)):seasonAssetUrl(iconFile)}" alt="">`:'';return`<span class="season-reward${iconFile?'':' text-only'}">${iconMarkup}<span>${safe(reward)}</span></span>`;}
function renderSeasonPass(){const season=window.RANGER_SEASON,container=$('#season-pass-content');if(!season||!container)return;const rows=season.rewards.map(([tier,free,premium,xp])=>`<tr><th scope="row">${tier}</th><td>${seasonReward(free)}</td><td>${seasonReward(premium)}</td><td>${xp.toLocaleString()}</td></tr>`).join('');container.innerHTML=`<div class="season-intro"><div><span class="season-number">Season ${season.number}</span><p>Earn rewards on the Free track or unlock the Premium track for additional rewards.</p></div><div class="season-infinite"><img src="${seasonAssetUrl('gadgetron_body.png')}" alt=""><div><strong>Infinite Reebos</strong><span>Every tier after 60 rewards ${safe(season.infiniteReward)}.</span></div></div></div><div class="season-rewards"><div class="season-table-wrap"><table class="season-table"><caption>Season ${season.number} rewards for tiers 0–60</caption><thead><tr><th scope="col">Tier</th><th scope="col">Free reward</th><th scope="col">Premium reward</th><th scope="col">XP</th></tr></thead><tbody>${rows}</tbody></table></div></div>`;}
const themeButton=$('#theme-toggle');function setTheme(theme){document.documentElement.dataset.theme=theme;localStorage.setItem('rangerRumbleTheme',theme);themeButton.textContent=theme==='dark'?'☀':'☾';themeButton.setAttribute('aria-label',theme==='dark'?'Switch to light mode':'Switch to dark mode');}renderSeasonPass();setTheme(localStorage.getItem('rangerRumbleTheme')||'dark');themeButton.onclick=()=>setTheme(document.documentElement.dataset.theme==='dark'?'light':'dark');
